# Equivalent result with no external scroll offset: the green flood covers the
# red backdrop exactly. The external scroll offset is normalized out of
# interning but does not move content on screen, so the filtered region sits at
# the same place as the un-offset green rect.
root:
  items:
    - type: rect
      color: green
      bounds: [0, 50, 100, 100]
